name: ci-tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  unit-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run unit tests (PoC)
        run: |
          echo "Running unit tests (PoC)"
          echo "No real tests configured, this is a demonstrator run."
